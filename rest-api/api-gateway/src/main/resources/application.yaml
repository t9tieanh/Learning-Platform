server:
  port: 8888

app:
  api-prefix: /api/v1

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.http.server.reactive: DEBUG
    org.springframework.web.reactive: DEBUG
    reactor.netty.http.client: DEBUG

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true

      routes:
        - id: user
          uri: http://localhost:8080
          predicates:
            - Path=${app.api-prefix}/user/**, ${app.api-prefix}/user**, ${app.api-prefix}/auth**, ${app.api-prefix}/auth/**
          filters:
            - StripPrefix=2

        - id: course
          uri: http://localhost:8081
          predicates:
            - Path=${app.api-prefix}/courses/**, ${app.api-prefix}/courses**
          filters:
            - StripPrefix=2